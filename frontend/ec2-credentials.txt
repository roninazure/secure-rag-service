# EC2 Deployment Credentials
# Fill in these values before deployment

# Pinecone Credentials (from your .env file)
PINECONE_API_KEY=your-pinecone-api-key-here
PINECONE_ENVIRONMENT=your-pinecone-environment-here

# EC2 Instance Details (after starting)
EC2_PUBLIC_IP=pending-instance-start
EC2_KEY_PATH=/path/to/Scott-Key.pem

# Commands to connect:
# ssh -i /path/to/Scott-Key.pem ubuntu@<EC2_PUBLIC_IP>
